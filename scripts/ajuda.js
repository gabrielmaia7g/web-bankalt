const formAjuda = document.querySelector('#formAjuda');
const btnEnviarMensagem = document.querySelector('#enviarMensagem');

formAjuda.addEventListener('submit', (evento) =>{
    evento.preventDefault();
})

btnEnviarMensagem.addEventListener('submit', (evento) =>{
    evento.preventDefault();
})